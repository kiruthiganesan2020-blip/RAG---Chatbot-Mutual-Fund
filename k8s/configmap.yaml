apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  ENVIRONMENT: "production"
  LOG_LEVEL: "INFO"
  CHROMA_DB_PATH: "/app/data/embeddings/chroma.db"
  REDIS_URL: "redis://redis-service:6379"
  # Add other configuration as needed
